# iOS Data Flow Diagram: Linked Homes System

## 🗺️ System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         MAP VIEW (MapKit)                            │
│                                                                      │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                │
│  │ 🏠   │  │ 🏠   │  │ 🏠   │  │ 🏠   │  │ 🏠   │  Buildings      │
│  │ Red  │  │Green │  │ Blue │  │Yellow│  │ Red  │  (Colored)      │
│  └──┬───┘  └──────┘  └──────┘  └──┬───┘  └──────┘                │
│     │                               │                               │
│     └───────────────────────────────┘                               │
│                    TAP                                               │
└─────────────────────┬───────────────────────────────────────────────┘
                      │
                      ▼
            ┌─────────────────┐
            │   GERS ID        │  (UUID from map tap)
            │   e.g.,          │
            │   089c3a7e-      │
            │   6f29-4e3b-     │
            │   a1de-          │
            │   b85f3c4a9d12   │
            └─────────┬────────┘
                      │
                      ▼
     ┌────────────────────────────────────────┐
     │    BUILDING DATA SERVICE (Swift)        │
     │    fetchBuildingData(gersId, campId)   │
     └────────────────┬───────────────────────┘
                      │
            ┌─────────┴─────────┐
            │                   │
            ▼                   ▼
    ┌───────────────┐   ┌───────────────┐
    │  Query Path 1 │   │  Query Path 2 │
    │  (Direct)     │   │  (via Links)  │
    └───────┬───────┘   └───────┬───────┘
            │                   │
            └─────────┬─────────┘
                      ▼
         ┌────────────────────────┐
         │  RESOLVED ADDRESS DATA  │
         │  • Address ID           │
         │  • Street               │
         │  • City, State, ZIP     │
         │  • QR Status            │
         │  • Scan Count           │
         └────────────┬───────────┘
                      │
            ┌─────────┴─────────┐
            │                   │
            ▼                   ▼
    ┌──────────────┐   ┌──────────────┐
    │  CONTACTS    │   │  QR STATUS   │
    │  • Residents │   │  • Has Flyer │
    │  • Phone     │   │  • Scans     │
    │  • Notes     │   │  • Last Scan │
    └──────┬───────┘   └──────┬───────┘
           │                  │
           └─────────┬────────┘
                     ▼
        ┌────────────────────────┐
        │   LOCATION CARD UI      │
        │                         │
        │  123 Main Street        │
        │  Toronto, ON M5V 2T6    │
        │                         │
        │  👥 John Doe + 1 other  │
        │  📱 QR Scanned 3x       │
        │                         │
        │  [Navigate] [Log Visit] │
        └─────────────────────────┘
```

## 🗄️ Database Table Relationships

```
┌──────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                 │
└──────────────────────────────────────────────────────────────────┘

┌────────────────────┐
│   map_buildings    │  ← Main building geometry table
├────────────────────┤
│ id (UUID) PK       │
│ gers_id (UUID) 🔑  │  ← UNIQUE IDENTIFIER (from Overture Maps)
│ geom (Polygon)     │
│ centroid (Point)   │
│ height_m           │
│ campaign_id        │
│ address_id         │
└─────┬──────────────┘
      │
      │ Links to:
      │
      ├─────────────────────────────────────────┐
      │                                         │
      ▼                                         ▼
┌────────────────────┐              ┌──────────────────────┐
│ building_address_  │              │  building_stats      │
│      links         │              ├──────────────────────┤
├────────────────────┤              │ building_id PK       │
│ id PK              │              │ gers_id 🔑           │
│ building_id FK ────┼──────────────┤ status               │
│ address_id FK      │              │ scans_total          │
│ campaign_id FK     │              │ scans_today          │
│ match_method       │              │ last_scan_at         │
│ is_primary         │              └──────────────────────┘
└─────┬──────────────┘
      │
      │
      ▼
┌────────────────────┐
│ campaign_addresses │  ← Address/business data
├────────────────────┤
│ id (UUID) PK 🔑    │  ← Used by contacts, QR codes
│ gers_id (UUID)     │  ← Also stores GERS ID for direct lookup
│ campaign_id FK     │
│ house_number       │
│ street_name        │
│ formatted          │
│ locality           │
│ region             │
│ postal_code        │
│ scans              │  ← Total scan count
│ last_scanned_at    │
│ qr_code_base64     │
└─────┬──────────────┘
      │
      │ Used by:
      │
      ├──────────────────┬──────────────────┐
      │                  │                  │
      ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  contacts    │  │  qr_codes    │  │ scan_events  │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ id PK        │  │ id PK        │  │ id PK        │
│ address_id FK│  │ address_id FK│  │ address_id FK│
│ gers_id      │  │ gers_id      │  │ building_id  │
│ full_name    │  │ image_data   │  │ campaign_id  │
│ phone        │  │ created_at   │  │ scanned_at   │
│ email        │  └──────────────┘  └──────────────┘
│ status       │
│ notes        │
└──────────────┘
```

## 🔄 Data Fetching Flow (Step by Step)

### Path 1: Direct GERS ID Lookup (Fastest)

```
User taps building
       ↓
┌─────────────────────────────────────────────────────┐
│ Step 1: Direct Campaign Address Lookup              │
├─────────────────────────────────────────────────────┤
│ SELECT * FROM campaign_addresses                    │
│ WHERE campaign_id = ?                               │
│ AND (gers_id = ? OR building_gers_id = ?)          │
└─────────────────┬───────────────────────────────────┘
                  │
      ┌───────────┴───────────┐
      │                       │
      ▼ (found)               ▼ (not found)
┌──────────────┐        Go to Path 2
│ Address Data │        (building_address_links)
└──────┬───────┘
       │
       ▼
┌────────────────────────────────────┐
│ Step 2: Fetch Related Data         │
├────────────────────────────────────┤
│ • Query contacts table              │
│   WHERE address_id = ?              │
│                                     │
│ • Parse QR status from address:     │
│   - qr_code_base64 field            │
│   - scans field                     │
│   - last_scanned_at field           │
└─────────────────┬──────────────────┘
                  │
                  ▼
        ┌───────────────────┐
        │  Display UI       │
        └───────────────────┘
```

### Path 2: Via Building Links (Fallback)

```
Direct lookup failed
       ↓
┌─────────────────────────────────────────────────────┐
│ Step 1: Find Building Record                        │
├─────────────────────────────────────────────────────┤
│ SELECT id, gers_id FROM map_buildings               │
│ WHERE gers_id = ?                                   │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ Step 2: Find Address Link                           │
├─────────────────────────────────────────────────────┤
│ SELECT address_id, campaign_addresses.*             │
│ FROM building_address_links                         │
│ JOIN campaign_addresses ON ...                      │
│ WHERE building_id = ?                               │
│ AND campaign_id = ?                                 │
│ AND is_primary = true                               │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
           (Continue to Step 2 from Path 1)
```

## 🎨 Building Color State Machine

```
┌─────────────────────────────────────────────────────────────┐
│             Building Status Priority Flow                    │
└─────────────────────────────────────────────────────────────┘

                    Check Properties:
                    • scans_total
                    • qr_scanned
                    • status

                          ↓
                          
    ┌─────────────────────────────────────────┐
    │  scans_total > 0  OR  qr_scanned=true?  │
    └───────┬─────────────────────────┬───────┘
            │ YES                     │ NO
            ▼                         ▼
    ┌─────────────┐          ┌────────────────┐
    │ QR_SCANNED  │          │  status='hot'? │
    │   YELLOW    │          └───────┬────────┘
    │  #FCD34D    │                  │ YES    │ NO
    │             │                  ▼        ▼
    │ Priority: 1 │          ┌──────────────────────┐
    └─────────────┘          │  CONVERSATIONS       │
                             │    BLUE              │
                             │   #3B82F6            │
                             │                      │
                             │  Priority: 2         │
                             └──────────────────────┘
                                        │ NO
                                        ▼
                             ┌──────────────────────┐
                             │  status='visited'?   │
                             └───────┬──────────────┘
                                     │ YES    │ NO
                                     ▼        ▼
                             ┌──────────────────────┐
                             │    TOUCHED           │
                             │    GREEN             │
                             │   #10B981            │
                             │                      │
                             │   Priority: 3        │
                             └──────────────────────┘
                                        │ NO
                                        ▼
                             ┌──────────────────────┐
                             │   UNTOUCHED          │
                             │     RED              │
                             │   #EF4444            │
                             │                      │
                             │   Priority: 4        │
                             │   (Default)          │
                             └──────────────────────┘
```

## 🔔 Real-time Update Flow

```
┌────────────────────────────────────────────────────────────┐
│                 Real-time Subscription                      │
└────────────────────────────────────────────────────────────┘

QR Code Scanned
on Physical Flyer
        ↓
┌───────────────────────────────────────┐
│  /app/api/scan/route.ts               │
│  (Backend API)                        │
│                                       │
│  1. Insert scan_events record         │
│  2. Update campaign_addresses.scans   │
│  3. Trigger updates building_stats    │
└────────────────┬──────────────────────┘
                 │
                 ▼
┌───────────────────────────────────────┐
│  PostgreSQL Trigger                   │
│  (trg_update_building_stats)          │
│                                       │
│  UPDATE building_stats                │
│  SET scans_total = scans_total + 1,   │
│      status = 'visited',              │
│      last_scan_at = NOW()             │
└────────────────┬──────────────────────┘
                 │
                 ▼
┌───────────────────────────────────────┐
│  Supabase Real-time                   │
│  (Postgres LISTEN/NOTIFY)             │
└────────────────┬──────────────────────┘
                 │
                 ▼
┌───────────────────────────────────────┐
│  Swift App (BuildingStatsSubscriber)  │
│                                       │
│  Receives:                            │
│  {                                    │
│    "gers_id": "uuid",                 │
│    "status": "visited",               │
│    "scans_total": 3,                  │
│    ...                                │
│  }                                    │
└────────────────┬──────────────────────┘
                 │
                 ▼
┌───────────────────────────────────────┐
│  Update Map Annotation                │
│                                       │
│  1. Find BuildingAnnotation by gers_id│
│  2. Update color: RED → YELLOW        │
│  3. Refresh annotation view           │
└───────────────────────────────────────┘
                 │
                 ▼
        User sees color change
        in real-time on map!
```

## 📊 Location Card UI Component Tree

```
┌──────────────────────────────────────────────────────┐
│              LocationCardView                         │
│  (Main Container - 320px width)                      │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  Header Section                             │    │
│  ├────────────────────────────────────────────┤    │
│  │  • Street Address (Headline)                │    │
│  │  • City, State, ZIP (Caption)               │    │
│  │  • Status Badge (Scanned/Target/New)        │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  Residents Row (Interactive)                │    │
│  ├────────────────────────────────────────────┤    │
│  │  👥 Icon (Blue circle)                      │    │
│  │  Name: "John Doe + 1 other"                 │    │
│  │  Count: "2 residents"                       │    │
│  │  → Taps: Edit contact / Add contact         │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  Notes Section (Conditional)                │    │
│  ├────────────────────────────────────────────┤    │
│  │  📝 Notes                                   │    │
│  │  "Interested in solar, call after 6pm"      │    │
│  │  (Only shown if any resident has notes)     │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  QR Status Row (Read-only)                  │    │
│  ├────────────────────────────────────────────┤    │
│  │  📱 Icon (Green/Orange/Gray circle)         │    │
│  │  Status: "Scanned 3x" or "Flyer delivered"  │    │
│  │  Detail: "Last: Feb 4, 2026"                │    │
│  │  ✓ Checkmark (if scanned)                   │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ─────────────────────────────────────────────     │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  Action Footer (Button Row)                 │    │
│  ├────────────────────────────────────────────┤    │
│  │  [🧭 Navigate] [📋 Log Visit] [➕ Add]      │    │
│  │  (Each button 1/3 width, evenly spaced)     │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
└──────────────────────────────────────────────────────┘
```

## 🧪 Testing Flow

```
┌────────────────────────────────────────────────────────┐
│                 Testing Strategy                        │
└────────────────────────────────────────────────────────┘

Unit Tests (XCTest)
├── BuildingDataService Tests
│   ├── ✓ Fetch with valid GERS ID
│   ├── ✓ Fetch with invalid GERS ID
│   ├── ✓ Handle network errors
│   └── ✓ Parse all data types correctly
│
├── Building Color Logic Tests
│   ├── ✓ QR scanned → Yellow (priority 1)
│   ├── ✓ Hot status → Blue (priority 2)
│   ├── ✓ Visited status → Green (priority 3)
│   └── ✓ Default → Red (priority 4)
│
└── Data Model Tests
    ├── ✓ Codable encoding/decoding
    ├── ✓ UUID parsing
    └── ✓ Optional field handling

Integration Tests
├── Mock Supabase Responses
│   ├── ✓ Campaign address query
│   ├── ✓ Building links query
│   ├── ✓ Contacts query
│   └── ✓ Real-time subscription events
│
└── Full Data Flow
    ├── ✓ GERS ID → Address resolution
    ├── ✓ Address → Contacts fetch
    └── ✓ Complete UI rendering

UI Tests (XCUITest)
├── Location Card Display
│   ├── ✓ Shows address correctly
│   ├── ✓ Shows resident count
│   ├── ✓ Shows QR status
│   └── ✓ Action buttons tappable
│
├── Loading States
│   ├── ✓ Shows loading indicator
│   ├── ✓ Handles errors gracefully
│   └── ✓ Shows unlinked building state
│
└── Real-time Updates
    ├── ✓ Building color changes on scan
    ├── ✓ Scan count increments
    └── ✓ Status updates reflect immediately
```

## 📱 iOS App Architecture

```
┌────────────────────────────────────────────────────────┐
│              iOS App Layer Structure                    │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────┐
│         Presentation Layer              │
├────────────────────────────────────────┤
│  • MapViewController                    │
│  • LocationCardView (SwiftUI)           │
│  • BuildingAnnotationView               │
│  • LoadingView, ErrorView               │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│         Service Layer                   │
├────────────────────────────────────────┤
│  • BuildingDataService                  │
│    - fetchBuildingData()                │
│    - @Published properties              │
│                                         │
│  • BuildingStatsSubscriber              │
│    - subscribe(campaignId)              │
│    - onUpdate callback                  │
│                                         │
│  • ColorManager                         │
│    - getColorForStatus()                │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│         Data Layer                      │
├────────────────────────────────────────┤
│  • SupabaseClient (singleton)           │
│  • Models:                              │
│    - ResolvedAddress                    │
│    - Contact                            │
│    - QRStatus                           │
│    - BuildingFeatureProperties          │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│         Network Layer                   │
├────────────────────────────────────────┤
│  • Supabase Swift SDK                   │
│    - .from() queries                    │
│    - .rpc() calls                       │
│    - .channel() subscriptions           │
└────────────────────────────────────────┘
```

## 🔑 Key IDs & Their Purpose

```
┌────────────────────────────────────────────────────────┐
│                 ID Hierarchy                            │
└────────────────────────────────────────────────────────┘

GERS ID (UUID)
├── Origin: Overture Maps global building identifier
├── Format: UUID v4 (128-bit)
├── Scope: Globally unique across all buildings
├── Used for: Map rendering, building selection
└── Example: "089c3a7e-6f29-4e3b-a1de-b85f3c4a9d12"
    ↓
    Links to...
    ↓
Address ID (UUID)
├── Origin: Campaign-specific address record
├── Format: UUID v4 (128-bit)
├── Scope: Unique within FLYR Pro database
├── Used for: Linking contacts, QR codes, business data
└── Example: "7f3c5a8d-9e12-4b3a-bc45-1d6e8f9a2c4b"
    ↓
    Links to...
    ↓
Contact IDs, QR Code IDs, Scan Event IDs
└── All business records reference address_id
```

## 💾 Caching Strategy

```
┌────────────────────────────────────────────────────────┐
│              Recommended Caching                        │
└────────────────────────────────────────────────────────┘

Building Data Cache
├── Key: "\(campaignId):\(gersId)"
├── TTL: 5 minutes
├── Invalidate on:
│   ├── Real-time update received
│   ├── User adds/edits contact
│   └── Manual refresh action
└── Storage: UserDefaults or in-memory dictionary

Map Annotations Cache
├── Key: campaignId
├── TTL: Until campaign changes
├── Contents: All building features for campaign
└── Storage: In-memory (MapView instance variable)

QR Code Images Cache
├── Key: addressId
├── TTL: 1 hour
├── Storage: NSCache
└── Max Size: 50 MB
```

---

This diagram reference provides a complete visual understanding of how data flows through your iOS app when implementing the linked homes and building stats card feature.
